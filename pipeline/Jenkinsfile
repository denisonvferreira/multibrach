pipeline {
    agent any
    stages {
        stage('Hello') {
               when {
                    anyOf { 
                        branch 'main*'
                        branch 'dev*'
                    }
               }
               steps {
                    echo "lendo branch  ${env.BRANCH_NAME}"
               }
        }
    }
}
