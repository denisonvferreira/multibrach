pipeline {
    agent any
    stages {
        stage('Hello') {
               when {
                    anyOf { 
                        branch 'main*'
                        branch 'dev*'
                    }
               }
            steps {
	    	    def jobName = env.JOB_NAME+"_"+new Date()
		        currentBuild.displayName = "$jobName"
		        node {
			        echo "job name $jobName"
		        }
        }
    }
}
