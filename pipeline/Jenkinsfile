pipeline {
    agent any

    stages {
        stage('Example') {
            steps {
                sh '''
                echo $(env.BRANCH_NAME)
                '''
            }
        }
    }
}
