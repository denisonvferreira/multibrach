pipeline {
    agent any
    stages {
        stage('Hello') {
               when {
                    anyOf { 
                        branch 'main*'
                        branch 'dev*'
                    }
               }
            steps {
		    script{
	    	    	def jobName = "teste-nome"+"_"+new Date()
		        	currentBuild.displayName = "$jobName"
		        	node {
			        	echo "job name $jobName"
		        	}
		    }
	    }
    
	}

    }
}
