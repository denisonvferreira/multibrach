pipeline {
    agent any
    stages {
        stage('Hello') {
               when {
                    anyOf { 
                        branch 'main*'
                        branch 'dev*'
                    }
               }
            steps {
		    script{
	    	    	def jobName = "teste-nome"+"_"+new Date()
// 				jobname = jobname.format("yyMMdd.HHmm", TimeZone.getTimeZone('UTC'))
// 		        	currentBuild.displayName = "$jobName"
		        	node {
			        	echo "job name $jobName"
		        	}
		    }
	    }
    
	}

    }
}
